content_roots = ["nodus"]
[mcp_servers.grapha]
command = "grapha"
args = ["serve", "--mcp", "--watch", "-p", "."]

[adapters]
enabled = ["claude", "codex"]
[launch_hooks]
sync_on_startup = true
[dev-dependencies]
fuli = { github = "oops-rs/fuli", branch = "main" }
