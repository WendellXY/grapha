---
description: Re-index the project with grapha
---
Run `grapha index .` and report the summary (nodes, edges, time).
Use this whenever symbol snippets or relationship queries look stale; indexing refreshes the stored full-symbol snippets used by `grapha symbol search --context` and `grapha symbol context`.
